include ../utils/mixins
block variables

doctype html
html(lang='he', dir="rtl")
	head
		meta(charset='utf-8')
		meta(name='viewport', content='width=device-width, initial-scale=1')
		title #{title}

	body
		.wrapper(class=light?'wrapper_light':'')
			main.main
				block content
